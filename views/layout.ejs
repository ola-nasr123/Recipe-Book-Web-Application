<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>Cooking recipes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body dir="rtl">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
  <a class="navbar-brand" href="/">My recipes</a>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav me-auto">
      <% if (user) { %>
        <li class="nav-item"><a class="nav-link" href="/recipes/dashboard">Control panel</a></li>
        <li class="nav-item"><a class="nav-link" href="/recipes/add">Add a recipe</a></li>
        <li class="nav-item"><a class="nav-link" href="/logout">Log out</a></li>
      <% } else { %>
        <li class="nav-item"><a class="nav-link" href="/login"></a>Login</li>
        <li class="nav-item"><a class="nav-link" href="/register"></a>Create an account</li>
      <% } %>
    </ul>
  </div>
</nav>

<div class="container mt-4">
  <% if (success_msg) { %>
    <div class="alert alert-success"><%= success_msg %></div>
  <% } %>
  <% if (error_msg) { %>
    <div class="alert alert-danger"><%= error_msg %></div>
  <% } %>

  <%- body %>
</div>

</body>
</html>